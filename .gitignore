# ---------------------------------------------------------------------------
# 0. Claude Code
# ---------------------------------------------------------------------------
.claude/

# ---------------------------------------------------------------------------
# 1. 기본 디렉토리 및 빌드 결과물
# ---------------------------------------------------------------------------
.vs/
DerivedDataCache/
Intermediate/
Saved/
Build/
.idea/
*.sln
*.suo
*.opensdf
*.sdf
*.xcodeproj
*.xcworkspace

# ---------------------------------------------------------------------------
# 2. Binaries 기본 처리
# ---------------------------------------------------------------------------
# 프로젝트 루트 Binaries 폴더 무시
Binaries/*

# 플러그인 빌드 중간산물만 무시 (.exp, .lib)
Plugins/**/Binaries/Win64/*.exp
Plugins/**/Binaries/Win64/*.lib
Plugins/**/Intermediate/

# ---------------------------------------------------------------------------
# 3. 프로젝트 루트 바이너리 허용 (에디터 실행에 필요)
# ---------------------------------------------------------------------------
!Binaries/Win64/
!Binaries/Win64/*.modules
!Binaries/Win64/*.target
!Binaries/Win64/*.dll
!Binaries/Win64/*.pdb

# ---------------------------------------------------------------------------
# 4. 플러그인 바이너리 허용 (비 개발자 컴퓨터에서 빌드 없이 열기 위해)
# ---------------------------------------------------------------------------
!Plugins/**/Binaries/Win64/*.dll
!Plugins/**/Binaries/Win64/*.pdb
!Plugins/**/Binaries/Win64/*.modules
