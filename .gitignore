# ---------------------------------------------------------------------------
# 1. 기본 언리얼 엔진 무시 항목 (Standard Unreal Engine Ignores)
# ---------------------------------------------------------------------------
.vs/
DerivedDataCache/
Intermediate/
Saved/
Build/
.idea/
*.sln
*.suo
*.opensdf
*.sdf
*.xcodeproj
*.xcworkspace

# ---------------------------------------------------------------------------
# 2. Binaries 폴더 처리 (핵심)
# ---------------------------------------------------------------------------
# 일단 Binaries 폴더의 모든 것을 무시합니다.
Binaries/*

# ---------------------------------------------------------------------------
# 3. 아티스트 공유를 위한 예외 처리 (Whitelisting)
# 개발자가 빌드한 DLL과 PDB를 공유하기 위해 아래 항목들은 무시하지 않음(!)
# ---------------------------------------------------------------------------

# Win64 폴더 자체는 추적
!Binaries/Win64/

# 언리얼이 모듈을 인식하는 데 필요한 파일
!Binaries/Win64/*.modules
!Binaries/Win64/*.target

# 실제 컴파일된 라이브러리 (DLL)
!Binaries/Win64/*.dll

# 디버깅 심볼 (PDB) - 에러 발생 시 로그 확인을 위해 필요 (용량이 큼)
!Binaries/Win64/*.pdb

# 실행 파일 (필요한 경우만 주석 해제, 보통 에디터 실행은 .uproject로 하므로 불필요할 수 있음)
#!Binaries/Win64/*.exe